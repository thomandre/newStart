		<div class="header">
			<div class="logo">
				<span class="logo"><a href="{{ path('feed') }}" style="text-align:left"><span class="glyphicon glyphicon-home"></span>HaveFyve</a></span>
			</div>
			<div class="menu">
				<span class="menu-links">
					<a href="{{ path('feed') }}"><span class="glyphicon glyphicon-calendar"></span>What's new ?</a>
				</span>
				<span class="menu-links">
					<a href="{{ path('friends') }}#/friends/"><span class="glyphicon  glyphicon-user"></span>Friends</a>
				</span>
				<span id="profile">
					<a href="{{ path('me') }}">
						{{ user.firstName }} {{ user.lastName }}
					</a>
				</span>
				<span>
					<a href="#" class="settings"><span class="glyphicon glyphicon-play"></span></a>
			        <ul class="ddown-menu">
			          <li><a href="#">Settings</a></li>
			          <li><a id="logout" href="{{ path('_security_logout') }}">Logout</a></li>
			        </ul>
				</span>
			</div>
			<div style="clear:both"></div>
		</div>
		<svg id="svg-effects" style="display:none">
		    <filter id="blur-effect-1">
		        <feGaussianBlur stdDeviation="0.9" />
		    </filter>

		    <filter id="blur-effect-2">
		        <feGaussianBlur stdDeviation="2" />
		    </filter>
		</svg> 

		<svg:svg>
			<!-- Filter ID/Name -->
			<svg:filter id="blur-effect">
				<!-- Amount of Gaussian Blur that should be applied -->
				<svg:feGaussianBlur stdDeviation="0.9"/>
			</svg:filter>
			<svg:filter id="example-two">
				<svg:feGaussianBlur stdDeviation="2"/>
	   		 </svg:filter>
		</svg:svg>

